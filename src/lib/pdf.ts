import type {ChartResult} from "@/types/chart";export function openPrintableReport(chart:ChartResult){const w=window.open("","_blank");if(!w)return;w.document.write(`<html><head><title>Hora Studio — ${chart.meta.name}</title><style>body{font-family:Georgia,serif;padding:32px}h1,h2{color:#6B1A1A}table{width:100%;border-collapse:collapse}th,td{border:1px solid #ccc;padding:6px;text-align:left}</style></head><body><h1>Hora Studio</h1><p>${chart.meta.name} · ${chart.meta.datetime} · ${chart.meta.place}</p><h2>Lagna — ${chart.lagna.sign} ${chart.lagna.degree}°</h2><table><tr><th>Planet</th><th>Sign</th><th>Degree</th><th>House</th><th>Nakshatra</th></tr>${chart.planets.map(p=>`<tr><td>${p.name}</td><td>${p.sign}</td><td>${p.degree}°${String(p.minute).padStart(2,"0")}'</td><td>${p.house}</td><td>${p.nakshatra} ${p.pada}</td></tr>`).join("")}</table><script>window.onload=()=>window.print()</script></body></html>`);w.document.close()}