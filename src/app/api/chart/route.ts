import{NextRequest,NextResponse}from"next/server";import type{BirthInput}from"@/types/chart";import{calculateChart}from"@/lib/ephemeris";export async function POST(req:NextRequest){try{const body=await req.json() as BirthInput;if(!body.name||!body.date||!body.time)return NextResponse.json({error:"name, date and time are required"},{status:400});return NextResponse.json(await calculateChart(body))}catch(e:any){return NextResponse.json({error:e.message||"Internal calculation error"},{status:500})}}