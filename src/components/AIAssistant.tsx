"use client";import{useState}from"react";import type{ChartResult}from"@/types/chart";export function AIAssistant({chart}:{chart:ChartResult|null}){const[open,setOpen]=useState(false);const[q,setQ]=useState("");const[a,setA]=useState("");if(!open)return <button onClick={()=>setOpen(true)} className="fixed bottom-6 right-6 z-40 btn-gold rounded-full">AI Assistant</button>;return <div className="fixed bottom-6 right-6 z-40 w-full max-w-md card-hora p-4"><div className="flex justify-between"><b className="text-gold-300">AI Assistant</b><button onClick={()=>setOpen(false)}>Close</button></div>{a&&<p className="my-3 text-sm whitespace-pre-wrap">{a}</p>}<form className="flex gap-2 mt-3" onSubmit={e=>{e.preventDefault();setA(chart?"Classical context for "+chart.meta.name+": "+chart.lagna.sign+" Lagna. Examine lordships, dignity and current dasha.":"Load a chart for context.");}}><input className="input-hora flex-1" value={q} onChange={e=>setQ(e.target.value)} placeholder="Ask about the chart…"/><button className="btn-gold">Ask</button></form></div>}